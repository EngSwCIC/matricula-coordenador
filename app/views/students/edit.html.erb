<div class="container pb-4">
	<h2 class="mt-4">Editar estudante</h2>
	<hr class="divider">

	<div class="card-body">
		<%= render partial: "form", locals: { aux: admins_student_path } %>
  </div>

	<%# link_to 'Mostrar', @coordinator %>
	<%= link_to 'voltar', admins_students_path %>
</div>