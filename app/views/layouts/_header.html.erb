<!-- Navigation --> 
<nav class="navbar navbar-expand-lg navbar-dark fixed-top" style="background-color: #37485A;">
  <div class="container">
    <a class="navbar-brand" href="/welcome/index">Matricula Coordenador</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarResponsive">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item active">
          <a class="nav-link" href="#">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">About</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Services</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Contact</a>
        </li>        

        <!-- link to backoffice if logged -->
        <li class="nav-item">
          <% if user_signed_in? %>
            <% if current_user.has_role? :admin %>
              <%= link_to  "Backoffice", admins_backoffice_path, class: "nav-item nav-link" %>
            <% end %>
          <% end %>
        </li>

        <li class="nav-item">
          <% if user_signed_in? %>
            <% if current_user.has_role? :coordinator %>
              <%= link_to  "Backoffice do Coordenador", coordinators_backoffice_path, class: "nav-item nav-link" %>
            <% end %>
          <% end %>
        </li>

        <!-- link to login -->
        <li class="nav-item">
          <% if user_signed_in? %>
          <%= link_to  "Logout", destroy_user_session_path, method: :delete, class: "nav-item nav-link" %>
        </li>
          <% else %>
          <%= link_to  "Login", new_user_session_path, class: "nav-item nav-link" %>
        </li>
        <% end %>

        <!-- account info and link to login -->
        <% if user_signed_in? %>
          <div class="nav-link">
            <div class="dropdown">
              <div  data-toggle="dropdown" id="dropdownMenuButton" aria-haspopup="true" aria-expanded="false">
                <li class="nav-item dropdown-toggle">Olá, <%= current_user.name.partition(" ").first %> </li>
              </a>
              <div class="dropdown-menu" id="dropdownMenuButton" aria-labelledby="dropdownMenuButton" style="margin-top: 15px;">
                <a class="dropdown-item" href="#"><i class="fa fas fa-cog fa-xs"></i> Preferências</a>
                <a class="dropdown-item" href="#"><i class="fa fas fa-user fa-xs"></i> <%= current_user.roles_name[0] %></a>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="#"><i class="fa fas fa-power-off fa-xs"></i> Logout</a>
              </div>
            </div>
          </div>
        <% else %>
          <li class="nav-item">
            <%= link_to  "Login", new_user_session_path, class: "nav-item nav-link" %>
          </li>
        <% end %>

      </ul>
    </div>
  </div>
</nav>